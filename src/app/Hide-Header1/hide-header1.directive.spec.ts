import { HideHeader1Directive } from './hide-header1.directive';

describe('HideHeader1Directive', () => {
  it('should create an instance', () => {
    const directive = new HideHeader1Directive();
    expect(directive).toBeTruthy();
  });
});
